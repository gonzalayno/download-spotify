[phases.setup]
nixPkgs = ["python3", "python3-pip", "ffmpeg"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build completed - Spotify Downloader ready'"]

[start]
cmd = "python app.py"
